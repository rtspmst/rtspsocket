<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray"
    tools:context=".activity.VideoPlaybackActivity">

    <RelativeLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="#0599E1">

        <!--视频回放-->
        <TextView
            android:id="@+id/tvBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:drawableLeft="@drawable/click_back_selector"
            android:gravity="center"
            android:text="视频回放"
            android:textColor="@color/white"
            android:textSize="22sp" />

        <!--删除-->
        <TextView
            android:id="@+id/tvDelete"
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:layout_gravity="right"
            android:layout_marginRight="20dp"
            android:clickable="true"
            android:drawableTop="@drawable/ic_delete_selector"
            android:gravity="center"
            android:text="删除"
            android:textColor="@drawable/text_color_selector"
            android:textSize="20sp" />

        <!--全选-->
        <TextView
            android:id="@+id/btnSelectAll"
            android:layout_width="120dp"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_gravity="right"
            android:layout_toLeftOf="@+id/tvDelete"
            android:clickable="true"
            android:drawableTop="@drawable/ic_select_all"
            android:gravity="center"
            android:text="全选"
            android:textColor="@drawable/text_color_selector"
            android:textSize="20sp" />
    </RelativeLayout>

    <com.fhc.laser_monitor_sw_android_rtsp_app.widget.EmptyRecyclerView
        android:id="@+id/recylerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar"
        android:layout_margin="3dp"
        android:background="@color/gray" />

    <include
        layout="@layout/emptyview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/toolbar" />

</RelativeLayout>
